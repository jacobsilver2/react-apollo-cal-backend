# import * from './generated/prisma.graphql'

type Mutation {
  createEvent(title: String, description: String, image: String, largeImage: String, date: DateTime): Event!
}

type Query {
  events: [Event]!
}