type User {
  id: ID! @unique
  name: String!
  email: String!
}

type Event {
  id: ID! @unique
  title: String!
  description: String
  image: String
  largeImage: String
  date: DateTime!
  createdAt: DateTime!
  updatedAt: DateTime!
  # user: User!
}
